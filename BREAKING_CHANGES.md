# Breaking Changes

## Dict-Mini.json

修改时间（北京时间）：2026-04-08 16:23:58 CST

译名列表不再只按总出现次数排序。

现在的排序顺序是：
- 先看该译名在各个游戏版本里的占比
- 再给较新的 MC 主版本一点权重
- 再看出现过多少个版本
- 最后看总出现次数

影响：
- 即使译名集合不变，列表里的第一项也可能变化。
- 如果你的代码把第一项当成“总出现次数最多的译名”，这个假设现在不成立。

如果你需要按总出现次数排序，请读取 `Dict.json` 或 `Dict-Sqlite.db` 后自行排序。
